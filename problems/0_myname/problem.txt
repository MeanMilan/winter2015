Write a module that export a JSON with `username` property of type string.
It will be your name in the competition.
Do not use bad words please : )

Save that code into a file, YOUR_PROGRAM.js (you can name it whatever you like)
and then:

  Use `$ADVENTURE_COMMAND verify YOUR_PROGRAM.js` to verify your program.
  Use `$ADVENTURE_COMMAND run YOUR_PROGRAM.js` to run your program.

-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
Example module exercise.js:

var myJSON = getVeryComplexJSON();
module.exports = myJSON;
