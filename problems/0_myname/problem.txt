Write a module that export a object JSON with `username` property of type string.
It will be your name in the competition.
Do not use bad words please : )

Save that code into a file, YOUR_PROGRAM.js (you can name it whatever you like)
and then:

  Use `mm-xmas verify YOUR_PROGRAM.js` to verify your program.
  Use `mm-xmas run YOUR_PROGRAM.js` to run your program.

-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
Example module exercise.js:

var myJSON = {...}
module.exports = myJSON;
