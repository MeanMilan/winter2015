Export a function that
given an undefined number of arguments
return true if all of them are 0.
The program must be less then 140 characters!


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
Example module exercise.js:

module.exports = function all_zero(arguments) {

  // HINT: google for 'javascript arguments'
  //       if you don't have any good ideas

  var ret = // true or false?

  return ret;
}
